import React from "react";

import Form from "@/components/dashboard/Form";
const page = () => {
    return (
        <div className="bg-black">
            <Form />
        </div>
    );
};

export default page;
